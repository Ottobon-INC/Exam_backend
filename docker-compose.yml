version: '3.8'

services:
  backend:
    build: .
    image: exam-backend
    container_name: exam-backend
    restart: unless-stopped
    ports:
      - "4010:4000"
    env_file:
      - .env
    network_mode: bridge
